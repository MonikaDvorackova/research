\section{Evaluated Systems and Setup}
\label{sec:systems}
\label{sec:setup}

Systems are reference implementations, not claimed winners: B1 compact flat, B2 latest-only, B3 recency-$k$, B4 BM25-$k$, H0 hierarchical (cap150, ret8).

\paragraph{Readers.} Symbolic exact-match on the full freeze. LLM Option~A: \texttt{Qwen2.5-3B-Instruct-4bit} via MLX; temp~$0$; max tokens~$48$; frozen JSON prompt; evidence packs $\le$3; H4 only. Confirmatory (not Option~A): parser re-scoring of frozen Qwen raw outputs; proprietary-reader transfer check with requested alias \texttt{gpt-4.1-mini} on a frozen stratified H0 subset (Section~\ref{sec:prop-transfer}). Fairness caveats: Appendix~\ref{app:fairness}. Frozen vs confirmatory never pooled.

